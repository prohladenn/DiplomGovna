<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyApplication" parent="Base.Theme.MyApplication" />

    <style name="Theme.Printhub" parent="Base.Theme.MyApplication" />

    <style name="BottomNavTextBold" parent="@android:style/TextAppearance.Material.Body1">
        <item name="android:textStyle">bold</item>
        <item name="android:fontFamily">monospace</item>
    </style>
    <style name="Widget.BottomNavigationView.Colored" parent="Widget.Material3.BottomNavigationView">
        <item name="android:background">@color/green_accent</item>
        <item name="itemIconTint">@color/green_accent</item>
        <item name="itemTextColor">@color/black</item>
        <item name="android:minHeight">42dp</item>
        <item name="android:paddingTop">0dp</item>
        <item name="android:paddingBottom">0dp</item>
        <item name="itemActiveIndicatorStyle">@null</item>
        <item name="android:textSize">16.8sp</item>
        <item name="itemTextAppearanceActive">@style/BottomNavTextBold</item>
        <item name="itemTextAppearanceInactive">@style/BottomNavTextBold</item>
    </style>
</resources>